apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cnpg-database.fullname" . }}-test-connection"
  labels:
    {{- include "cnpg-database.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cnpg-database.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
